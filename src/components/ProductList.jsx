import React from "react";

const ProductList = () => {
  // Tempat untuk logic (misal, fetch data produk)

  return (
    <div>
      <h2>Daftar Produk</h2>
      {/* Render daftar produk di sini */}
    </div>
  );
};

export default ProductList;
